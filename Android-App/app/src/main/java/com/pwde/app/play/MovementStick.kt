package com.pwde.app.play

/** How PWDe steers a game's movement joystick (a button whose trigger is Movement). */
object MovementStick {
    /** How far the stick is dragged at full head deflection, as a share of the screen's short side. */
    const val REACH = 0.12f
}
